# Улучшение словаря и мультиязычность

Дата: 2026-02-06

## Обзор изменений

### Часть A: Улучшения словаря (7 пунктов)

1. Список слов отображается сразу при входе в раздел
2. Убрать ручную оценку сложности — автоматический SRS
3. Род существительных через артикль (le, la, l')
4. До 3 примеров предложений на каждое слово
5. Расширение базы до 700-800 слов
6. 3 новых типа упражнений
7. Мини-сессия закрепления после изучения новых слов

### Часть B: Мультиязычность (5 языков)

8. Поддержка 5 языков: французский, английский, испанский, немецкий, португальский
9. Полный функционал для каждого языка: словарь + грамматика + разговор
10. Переключатель языка в хедере, один язык на экране
11. Отдельные файлы данных по языкам
12. Миграция Supabase — поле language в таблицах прогресса

### Часть C: Редизайн главной страницы

13. Контент без прокрутки, карточки навигации 2x2
14. Убрать нижнюю панель навигации
15. "Начать разговор" → "Разговоры"
16. Новый раздел "Упражнения" — микс словарных и грамматических
17. Настройки — иконка в хедере рядом с именем
18. Навигация назад: стрелка ← в хедере + клик на "JuLang"

---

# Часть A: Улучшения словаря

## 1. Новая структура экрана Словаря

При входе в раздел "Словарь" пользователь сразу видит список слов. Режим "menu" убирается.

```
┌─────────────────────────────────────┐
│  [Новые (5)]      [Повторить (12)]  │   ← компактная панель сверху
├─────────────────────────────────────┤
│  Тема ▾  Статус ▾  Сложность ▾  Уровень ▾  │   ← фильтры
├─────────────────────────────────────┤
│  Показано: 247 из 750 слов          │
│                                     │
│  ✅ le cafe — кофе           ⭐⭐    │
│  📖 la maison — дом          ⭐      │   ← список с прокруткой
│  ❓ le travail — работа      ⭐⭐⭐   │
│  ...                                │
└─────────────────────────────────────┘
```

Изменения:
- Убираем режим "menu" из VocabularyScreen
- Список отображается по умолчанию
- Кнопки "Новые" и "Повторить" — компактная панель над фильтрами
- Счётчики в кнопках показывают доступные карточки
- В списке род отображается артиклем: **le** cafe, **la** maison

---

## 2. Карточка слова — род и примеры

### Отображение рода

Вместо `bonjour (masculine)` показываем артикль перед словом:

- Мужской род: **le** (le cafe, le livre)
- Женский род: **la** (la maison, la vie)
- Перед гласной: **l'** (l'eau, l'homme)
- Глаголы, прилагательные, выражения: без артикля

Формат одинаковый везде: в карточке, в списке, в упражнениях.

### Примеры предложений

До 3 примеров на каждое слово (вместо 1):

```
┌─────────────────────────────────┐
│         [изображение]           │
│                                 │
│     le cafe  🔊                 │
│     кофе                        │
│                                 │
│  ✅ Выучено    ⭐⭐   ▓▓▓▓░     │
│                                 │
│  Примеры:                       │
│  1. Je prends un cafe le matin. │
│     Я пью кофе по утрам.   🔊  │
│                                 │
│  2. Ce cafe est tres bon.       │
│     Этот кофе очень вкусный. 🔊│
│                                 │
│  3. Un cafe au lait, s'il       │
│     vous plait.                 │
│     Кофе с молоком, пожал-та.🔊│
│                                 │
│  [Практика]     [Следующее →]   │
└─────────────────────────────────┘
```

### Изменение модели данных

Поле `gender` заменяется на `article`:

```json
{
  "id": "v-a1-042",
  "french": "eau",
  "article": "l'",
  "russian": "вода",
  "type": "word",
  "level": "A1",
  "topic": "food",
  "difficulty": 1,
  "frequency": 5,
  "examples": [
    { "fr": "Je voudrais de l'eau.", "ru": "Я хотел бы воды." },
    { "fr": "L'eau est froide.", "ru": "Вода холодная." },
    { "fr": "Un verre d'eau, s'il vous plait.", "ru": "Стакан воды, пожалуйста." }
  ],
  "imageUrl": "/images/vocab/v-a1-042.jpg"
}
```

- `gender?: 'masculine' | 'feminine'` → `article?: 'le' | 'la' | "l'" | null`
- `example: string` + `exampleTranslation: string` → `examples: Array<{ fr: string, ru: string }>`

---

## 3. Типы упражнений

### 6 типов (3 текущих + 3 новых)

| Тип | Задание | Формат ответа |
|-----|---------|---------------|
| `fr_to_ru` | Показано французское слово → перевести | Выбор из 4 вариантов |
| `ru_to_fr` | Показан русский перевод → перевести | Выбор из 4 вариантов |
| `multiple_choice` | Показано французское → выбрать перевод | Выбор из 4 вариантов |
| `listening` | TTS произносит слово → что сказано? | Выбор из 4 вариантов |
| `fill_blank` | Предложение с ___ → вставить слово | Выбор из 4 вариантов |
| `write_word` | Показан русский → написать по-французски | Текстовый ввод |

### Интерфейс новых типов

**Аудирование (listening):**
```
🔊 [Нажми, чтобы прослушать]

○ кофе     ○ чай
○ молоко   ○ вода
```

**Вставить слово (fill_blank):**
```
Je prends un ___ le matin.

○ cafe     ○ the
○ lait     ○ eau
```

**Написать по памяти (write_word):**
```
кофе

Напишите по-французски:
[____________]      [Проверить]
```

---

## 4. SRS без ручной оценки

Убираем кнопки оценки (0/3/4/5) из интерфейса. SRS обновляется автоматически:

- Правильный ответ → `quality = 4` → интервал увеличивается
- Неправильный ответ → `quality = 0` → интервал сбрасывается

Алгоритм SM-2 остаётся без изменений, меняется только способ определения quality.

После ответа — авто-переход к следующему упражнению (без промежуточного шага оценки).

---

## 5. Поток после изучения новых слов

После показа 5 новых карточек запускается мини-сессия закрепления:

```
Новые слова (5 карточек)
  │
  │  Показ: le cafe → [перевернуть] → кофе + примеры
  │  Показ: la maison → [перевернуть] → дом + примеры
  │  ... (всего 5)
  │
  ▼
Мини-сессия закрепления (10 упражнений)
  │
  │  2 упражнения на каждое новое слово
  │  Порядок слов перемешан
  │
  ▼
Результат: "5 новых слов изучено! 8/10 правильно"
  │
  ▼
Слова добавляются в SRS-очередь повторения
```

Логика выбора упражнений в мини-сессии:
- 1-е упражнение на слово — лёгкий тип (выбор из вариантов: `fr_to_ru`, `multiple_choice`, `listening`)
- 2-е упражнение — сложный тип (`write_word`, `fill_blank`, `ru_to_fr`)
- Порядок слов перемешивается (не два упражнения подряд на одно слово)

---

## 6. Расширение базы слов до 700-800

### Распределение по уровням

| Уровень | Кол-во слов | Описание |
|---------|-------------|----------|
| A1 | ~180 | Базовая лексика: приветствия, числа, еда, семья |
| A2 | ~200 | Повседневные ситуации: покупки, транспорт, здоровье |
| B1 | ~200 | Расширенная: работа, мнения, новости, отношения |
| B2 | ~150 | Продвинутая: абстрактные понятия, идиомы, нюансы |

### Расширение тем (8 → 12)

Текущие: `daily`, `food`, `travel`, `work`, `home`, `nature`, `emotions`, `communication`

Новые:
- `health` — здоровье и тело
- `education` — учёба и образование
- `shopping` — покупки и деньги
- `culture` — культура и развлечения

~60-70 слов на каждую тему, равномерно по уровням.

### Обновление типа VocabularyTopic

```typescript
type VocabularyTopic =
  | 'daily' | 'food' | 'travel' | 'work'
  | 'home' | 'nature' | 'emotions' | 'communication'
  | 'health' | 'education' | 'shopping' | 'culture'
```

### Генерация контента

- Слова генерируются через AI партиями по ~100 штук за тему/уровень
- Каждое слово с 3 примерами предложений
- Примеры по возрастанию сложности внутри уровня
- Генерация — отдельный этап, не блокирует разработку UI

---

## 7. Затрагиваемые файлы (часть A)

### Фаза 1 — Данные и типы
- `src/types/index.ts` — обновить VocabularyCard, VocabularyExerciseType, VocabularyTopic
- `src/data/vocabulary.json` — обновить структуру, расширить партиями

### Фаза 2 — Движок и логика
- `src/modules/VocabularyEngine.ts` — новые типы упражнений, логика мини-сессии, авто-SRS

### Фаза 3 — UI компоненты
- `src/components/Vocabulary/VocabularyScreen.tsx` — убрать "menu", список по умолчанию, кнопки сверху, поток мини-сессии
- `src/components/Vocabulary/WordCard.tsx` — артикль, 3 примера
- `src/components/Vocabulary/VocabularyListItem.tsx` — артикль в строке
- `src/components/Vocabulary/ExerciseCard.tsx` — 3 новых типа, убрать кнопки оценки
- `src/components/Vocabulary/ReviewSession.tsx` — убрать шаг оценки, авто-переход
- `src/components/Vocabulary/NewCardView.tsx` — запуск мини-сессии после 5 карточек
- `src/components/Vocabulary/VocabularyFilters.tsx` — 4 новые темы

### Фаза 4 — Генерация контента
- Генерация 700-800 слов через AI
- Валидация и запись в vocabulary.json

### Не меняется
- Supabase схема (vocabulary_progress) — обновится в части B
- userDataService.ts — обновится в части B
- useVocabularyFilters.ts (только список тем расширяется)
- SRS алгоритм SM-2

---

# Часть B: Мультиязычность

## 8. Архитектура мультиязычности

### 5 языков

| Код | Язык | Артикли (род) | TTS код |
|-----|------|---------------|---------|
| `fr` | Французский | le, la, l' | `fr-FR` |
| `en` | Английский | — (без рода) | `en-US` |
| `es` | Испанский | el, la | `es-ES` |
| `de` | Немецкий | der, die, das | `de-DE` |
| `pt` | Португальский | o, a | `pt-BR` |

### Тип Language

```typescript
type Language = 'fr' | 'en' | 'es' | 'de' | 'pt'
```

### Файловая структура данных

```
src/data/
├── fr/
│   ├── vocabulary.json      (~700-800 слов)
│   └── grammar-topics.json
├── en/
│   ├── vocabulary.json
│   └── grammar-topics.json
├── es/
│   ├── vocabulary.json
│   └── grammar-topics.json
├── de/
│   ├── vocabulary.json
│   └── grammar-topics.json
└── pt/
    ├── vocabulary.json
    └── grammar-topics.json
```

### Поле article по языкам

- FR: `'le' | 'la' | "l'" | null`
- DE: `'der' | 'die' | 'das' | null`
- ES: `'el' | 'la' | null`
- PT: `'o' | 'a' | null`
- EN: всегда `null`

### Supabase миграция

```sql
-- 002_add_language.sql
ALTER TABLE vocabulary_progress ADD COLUMN language TEXT NOT NULL DEFAULT 'fr';
ALTER TABLE grammar_cards ADD COLUMN language TEXT NOT NULL DEFAULT 'fr';
ALTER TABLE conversations ADD COLUMN language TEXT NOT NULL DEFAULT 'fr';
ALTER TABLE user_progress ADD COLUMN language TEXT NOT NULL DEFAULT 'fr';
ALTER TABLE user_profiles ADD COLUMN active_language TEXT DEFAULT 'fr';

CREATE INDEX idx_vocabulary_progress_language ON vocabulary_progress(user_id, language);
CREATE INDEX idx_grammar_cards_language ON grammar_cards(user_id, language);
```

Все существующие данные получают `language = 'fr'` — обратная совместимость.

---

## 9. UI — переключатель языка

### Хедер — переключатель рядом с именем

```
┌─────────────────────────────────────────┐
│  JuLang              [🇫🇷 ▾]  Алексей  │
└─────────────────────────────────────────┘
                         │
                    ┌────▼────┐
                    │ 🇫🇷 FR  ✓│
                    │ 🇬🇧 EN   │
                    │ 🇪🇸 ES   │
                    │─────────│
                    │ + Ещё   │
                    └─────────┘
```

- Показывает флаг текущего языка рядом с именем
- Клик — дропдаун только из ранее выбранных языков
- "+ Ещё" — добавить новый язык (онбординг уровня)
- При смене — весь интерфейс переключается мгновенно

### Главный экран — всегда один язык

```
┌─────────────────────────────────────────┐
│  JuLang              [🇫🇷 ▾]  Алексей  │
├─────────────────────────────────────────┤
│                                         │
│  Уровень: B1                            │
│  Слов изучено: 47 / 750                 │
│  К повторению сегодня: 12               │
│  Грамматика: 8 тем пройдено            │
│                                         │
│  [Словарь]  [Грамматика]  [Разговор]   │
│                                         │
└─────────────────────────────────────────┘
```

### Логика

- При входе в приложение — показываем **последний активный язык** пользователя
- Все разделы (словарь, грамматика, разговор) работают в контексте текущего языка
- Переключил язык сверху → статистика, слова, грамматика, разговор — всё обновляется
- Последний выбранный язык сохраняется в `user_profiles.active_language`
- Список доступных языков определяется по наличию записей прогресса

### Onboarding

При первом добавлении нового языка:
- Выбор уровня (A1-C2) для этого языка
- Создаётся запись в user_progress с language

---

## 10. Адаптация модулей

Все модули получают параметр `language`:

### VocabularyEngine.ts
- `getAllVocabularyCards(language)` — загружает `src/data/{language}/vocabulary.json`
- `getNewCards(userId, level, language)` — фильтрует по языку
- `getReviewQueue(userId, language)` — очередь только по текущему языку
- `getMultipleChoiceOptions(card, field, language)` — дистракторы из того же языка

### GrammarEngine.ts
- `getGrammarTopics(language)` — загружает `src/data/{language}/grammar-topics.json`
- Грамматические темы полностью разные для каждого языка
- SRS-карточки грамматики фильтруются по `language`

### AIService.ts
- Системный промпт параметризуется языком:
  - FR: *"Ты — преподаватель французского для русскоговорящих..."*
  - DE: *"Ты — преподаватель немецкого для русскоговорящих..."*
- Язык пользователя (русский) остаётся фиксированным

### SpeechService.ts
- TTS voice выбирается по языку: `fr-FR`, `en-US`, `es-ES`, `de-DE`, `pt-BR`
- `speak(text, language)` — добавляется параметр языка

### AuthContext / AppState
- Хранит `currentLanguage` — текущий выбранный язык
- При переключении — перезагружаются данные модулей
- Синхронизируется с `user_profiles.active_language`

### userDataService.ts
- Все запросы добавляют `.eq('language', currentLanguage)`
- `getVocabularyProgress(userId, language)`
- `getGrammarCards(userId, language)`
- `getConversations(userId, language)`

---

## 11. Затрагиваемые файлы (часть B)

### Фаза 1 — Фундамент
- `src/types/index.ts` — тип `Language`, поле `language` в интерфейсах
- `supabase/migrations/002_add_language.sql` — миграция БД
- `src/data/` — реорганизация в `src/data/fr/`, перенос существующих файлов

### Фаза 2 — Модули
- `src/modules/VocabularyEngine.ts` — параметр `language` во всех методах
- `src/modules/GrammarEngine.ts` — загрузка данных по языку
- `src/modules/AIService.ts` — промпты по языку
- `src/modules/SpeechService.ts` — TTS по языку
- `src/services/userDataService.ts` — фильтрация по `language`

### Фаза 3 — UI
- `src/contexts/AuthContext.tsx` — `currentLanguage` + переключение
- `src/components/Home/` — статистика по текущему языку
- Новый компонент: `LanguageSwitcher` — дропдаун в хедере
- `src/components/Onboarding/` — выбор языков + уровень для каждого
- `src/components/Settings/` — управление языками

### Фаза 4 — Контент
- Генерация vocabulary.json для `en`, `es`, `de`, `pt` (по 700-800 слов)
- Генерация grammar-topics.json для каждого языка

### Не меняется
- SRS алгоритм SM-2
- Supabase RLS политики (только фильтр по language добавляется)
- Общая структура компонентов словаря, грамматики, разговора

---

# Часть C: Новый интерфейс главной страницы

## 12. Редизайн главного экрана

### Принцип: всё на одном экране без прокрутки

Весь контент главной страницы помещается в видимую область — без скролла.

### Хедер — единый для всех экранов

На главной:
```
┌─────────────────────────────────────────┐
│  JuLang        [🇫🇷 ▾]  Алексей  [⚙️]  │
└─────────────────────────────────────────┘
```

В разделе:
```
┌─────────────────────────────────────────┐
│  [←] Словарь   [🇫🇷 ▾]  Алексей  [⚙️]  │
└─────────────────────────────────────────┘
```

- "JuLang" — кликабельный, всегда ведёт на главную
- Стрелка `←` появляется только внутри разделов (дополнительная навигация назад)
- `[🇫🇷 ▾]` — переключатель языка
- `[⚙️]` — иконка настроек (весь функционал текущего раздела Настройки)

### Главная страница — сетка 2x2

```
┌─────────────────────────────────────────┐
│  JuLang        [🇫🇷 ▾]  Алексей  [⚙️]  │
├─────────────────────────────────────────┤
│                                         │
│  Уровень: B1        Слов: 47 / 750     │
│  Повторить: 12      Грамматика: 8 тем  │
│                                         │
│  ┌─────────────┐  ┌─────────────┐      │
│  │  💬          │  │  📖          │      │
│  │  Разговоры   │  │  Словарь    │      │
│  │             │  │             │      │
│  └─────────────┘  └─────────────┘      │
│                                         │
│  ┌─────────────┐  ┌─────────────┐      │
│  │  📝          │  │  🏋️          │      │
│  │  Грамматика  │  │  Упражнения │      │
│  │             │  │             │      │
│  └─────────────┘  └─────────────┘      │
│                                         │
└─────────────────────────────────────────┘
```

### 4 карточки навигации

| Карточка | Ведёт в | Описание |
|----------|---------|----------|
| Разговоры | AI-беседа | Бывший "Начать разговор" |
| Словарь | Экран словаря | Список слов, новые, повторение |
| Грамматика | Грамматические темы | Теория и SRS-карточки |
| Упражнения | Единая сессия практики | Микс словарных и грамматических упражнений |

### Раздел "Упражнения" — новый

Единая сессия, которая берёт из двух очередей:
- Словарные упражнения (6 типов из части A)
- Грамматические упражнения (из PracticeEngine)
- Случайный порядок, чередуя словарь и грамматику
- Показывает общий прогресс: "12/20 · 9 правильно"

### Что убираем

- Нижняя панель навигации (Главная, Диалоги, Грамматика, Настройки) — полностью
- Отдельный экран/таб настроек — переносим в иконку `⚙️` в хедере
- Вертикальный список карточек — заменяем на сетку 2x2
- Прокрутку на главной странице

### Настройки через иконку в хедере

Клик на `⚙️` открывает страницу настроек (тот же функционал):
- Выбор AI-модели (OpenAI / Claude)
- API ключи
- Голос TTS
- Управление языками
- Выход из аккаунта

---

## 13. Затрагиваемые файлы (часть C)

- `src/components/Home/` — редизайн: сетка 2x2, компактная статистика
- `src/App.tsx` — убрать нижнюю навигацию, добавить роут для упражнений
- Новый компонент: `Header.tsx` — единый хедер с навигацией, языком, настройками, LanguageSwitcher
- Новый компонент: `ExercisesScreen.tsx` — микс словарных и грамматических упражнений
- `src/components/Settings/` — без изменений в функционале, только доступ через хедер
- Удалить: компонент нижней навигации (BottomNav / TabBar)
