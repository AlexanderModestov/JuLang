# Дизайн расширенного грамматического контента

Дата: 2026-02-14

## Цель

Сделать объяснения грамматических правил исчерпывающими: с формулами образования, случаями использования, исключениями, большим количеством примеров, типичными ошибками и мнемоническими подсказками.

## Текущее состояние

Каждая тема содержит ~400 символов:
- `rule` — 1-2 предложения (~140 символов)
- `examples` — 3 пары фр→рус
- `commonMistakes` — 2 ошибки

Этого недостаточно для серьёзного изучения грамматики.

## Новая структура контента

### Секции

| Поле | Тип | Обязательное | Описание |
|------|-----|-------------|----------|
| rule | string | да | Основное объяснение правила (300-500 символов) |
| formation | object | да | Формула образования: description, formula, details[] |
| usage | string[] | да | Когда и как используется (3-4 пункта) |
| exceptions | string[] | нет | Исключения из правила (2-4 пункта, где есть) |
| examples | {fr, ru}[] | да | 5-7 примеров с переводом |
| commonMistakes | string[] | да | Типичные ошибки (3-4 штуки) |
| tips | string[] | нет | Мнемоника и лайфхаки (1-3 подсказки, где есть) |

### Пример полной темы

```json
{
  "id": "passe-compose-avoir",
  "title": "Le passé composé avec avoir",
  "titleRu": "Прошедшее время с avoir",
  "level": "A2",
  "category": "tenses",
  "group": "tenses",
  "content": {
    "rule": "Passé composé — прошедшее завершённое время. Обозначает действие, которое произошло в прошлом и завершилось. Образуется с помощью вспомогательного глагола avoir (или être) в настоящем времени + participe passé основного глагола.",

    "formation": {
      "description": "Подлежащее + avoir в настоящем времени + participe passé",
      "formula": "Sujet + ai/as/a/avons/avez/ont + participe passé",
      "details": [
        "Глаголы на -er: -é (manger → mangé)",
        "Глаголы на -ir: -i (finir → fini)",
        "Глаголы на -re: -u (vendre → vendu)"
      ]
    },

    "usage": [
      "Завершённое действие в прошлом: J'ai mangé une pomme.",
      "Последовательность действий: Il a ouvert la porte et il est entré.",
      "С маркерами времени: hier, la semaine dernière, il y a deux jours"
    ],

    "exceptions": [
      "Неправильные participe passé: avoir → eu, être → été, faire → fait, prendre → pris, voir → vu, écrire → écrit",
      "Согласование с COD перед глаголом: La lettre que j'ai écrite (feminine agreement)"
    ],

    "examples": [
      { "fr": "J'ai mangé une pomme.", "ru": "Я съел яблоко." },
      { "fr": "Tu as fini ton travail.", "ru": "Ты закончил свою работу." },
      { "fr": "Elle a pris le bus.", "ru": "Она села на автобус." },
      { "fr": "Nous avons vu ce film.", "ru": "Мы посмотрели этот фильм." },
      { "fr": "Ils ont fait leurs devoirs.", "ru": "Они сделали домашнее задание." }
    ],

    "commonMistakes": [
      "Неправильный вспомогательный: Je suis mangé → J'ai mangé (manger использует avoir)",
      "Забыть participe passé: J'ai mange → J'ai mangé (нужен accent)",
      "Путать с imparfait: J'ai mangé (один раз, завершено) vs Je mangeais (регулярно, незавершено)"
    ],

    "tips": [
      "Большинство глаголов используют avoir. С être идут только глаголы движения/состояния и возвратные.",
      "Для запоминания неправильных participe passé группируйте по окончаниям: -is (pris, mis), -it (dit, écrit), -u (vu, lu, bu)"
    ]
  }
}
```

## Отображение — TopicDetail с аккордеоном

### Макет экрана

```
┌──────────────────────────────────┐
│  ← Назад                   A2   │
│                                  │
│  Le passé composé avec avoir     │
│  Прошедшее время с avoir    🔊   │
│                                  │
│  ▼ Правило              ────────│
│  │ Passé composé — прошедшее    │
│  │ завершённое время. Обозначает│
│  │ действие, которое произошло  │
│  │ в прошлом и завершилось...   │
│                                  │
│  ▶ Образование                  │
│  ▶ Использование                │
│  ▶ Исключения                   │
│  ▶ Примеры (5)                  │
│  ▶ Типичные ошибки              │
│  ▶ Подсказки                    │
│                                  │
│  [Практика]    [Следующее правило]│
└──────────────────────────────────┘
```

### Логика аккордеона

- "Правило" раскрыто по умолчанию
- Остальные секции свёрнуты
- Можно раскрыть несколько одновременно
- В скобках — количество элементов: "Примеры (5)", "Исключения (3)"
- Секции без данных (опциональные `exceptions`, `tips`) не отображаются вовсе

### Стили секций

- **Правило** — текст абзацем
- **Образование** — `description` курсивом, `formula` моноширинным шрифтом в выделенном блоке, `details` маркированным списком
- **Использование** — маркированный список, в каждом пункте пример выделен курсивом
- **Исключения** — маркированный список с красным акцентом слева (как предупреждение)
- **Примеры** — карточки: французский текст + кнопка 🔊 + русский перевод серым
- **Типичные ошибки** — формат "неправильно → правильно", неправильное зачёркнуто красным, правильное зелёным
- **Подсказки** — блок с иконкой 💡, фон слегка жёлтый

## Объём контента

### Текущий vs новый

| | Сейчас | Станет |
|---|--------|--------|
| rule | ~140 символов | ~300-500 символов |
| formation | нет | description + formula + 3-5 details |
| usage | нет | 3-4 пункта |
| exceptions | нет | 2-4 пункта (где есть) |
| examples | 3 пары | 5-7 пар |
| commonMistakes | 2 ошибки | 3-4 ошибки |
| tips | нет | 1-3 подсказки (где есть) |
| **Итого на тему** | ~400 символов | ~2000-3000 символов |

### Общий объём

- Французский: 31 тема × ~2500 символов = ~77 KB
- Английский: 28 тем × ~2500 символов = ~70 KB
- Текущие файлы: ~30 KB → вырастут до ~80-100 KB

### Порядок наполнения

1. A1 (7 фр + 7 англ) — базовые правила, самые востребованные
2. A2 (7 фр + 6 англ)
3. B1 → B2 → C1 → C2

## Обратная совместимость

Компонент проверяет наличие новых полей:
- `content.formation ? renderFormation() : null`
- `content.usage ? renderUsage() : null`
- и т.д.

Темы без новых полей отображаются как раньше. Можно обновлять постепенно, не все сразу.

## Изменяемые файлы

### Данные

```
src/data/grammar-topics.json         — расширить content для всех 31 темы (фр)
src/data/grammar-topics-en.json      — расширить content для всех 28 тем (англ)
```

### Типы

```
src/types/index.ts                   — обновить интерфейс GrammarTopicContent:

// Сейчас:
interface GrammarTopicContent {
  rule: string
  examples: { fr: string; ru: string }[]
  commonMistakes: string[]
}

// Станет:
interface GrammarTopicContent {
  rule: string
  formation: {
    description: string
    formula: string
    details: string[]
  }
  usage: string[]
  exceptions?: string[]
  examples: { fr: string; ru: string }[]
  commonMistakes: string[]
  tips?: string[]
}
```

### Компоненты

```
src/components/Grammar/TopicDetail.tsx         — переписать: аккордеон вместо
                                                 плоского вывода, рендер новых секций,
                                                 стили для каждого типа блока
src/components/GrammarReview/ReviewScreen.tsx   — адаптировать отображение карточки:
                                                 показывать rule + formation (кратко)
src/components/GrammarReview/CardDetailScreen.tsx — аналогично TopicDetail: аккордеон
```

### Без изменений

- `GrammarScreen.tsx` — список тем, не показывает контент
- `TopicGroup.tsx`, `TopicListItem.tsx` — навигация
- `GrammarEngine.ts` — берёт content целиком
- API-эндпоинты — не затрагиваются
- Упражнения, словарь, разговоры — без изменений
