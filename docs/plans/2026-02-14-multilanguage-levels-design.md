# Дизайн мультиязычной системы уровней

Дата: 2026-02-14

## Цель

Реализовать корректную логику работы уровней для разных языков изучения. Каждый язык имеет свой уровень владения (A1-C2). При переключении языка в хедере система адаптирует все экраны под выбранный язык и его уровень.

## Пользовательский сценарий

1. Пользователь в настройках выбирает языки для изучения
2. Пользователь устанавливает текущий уровень владения для каждого языка
3. Пользователь сохраняет
4. Активный язык определяется по переключателю в хедере рядом с именем пользователя
5. Чтобы сменить язык тренировки — переключить язык в хедере
6. Главный экран и все разделы перестраиваются на сессию изучения выбранного языка и уровня

## Таблица `user_language_settings`

Новая таблица в Supabase:

```sql
create table user_language_settings (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references auth.users(id) on delete cascade,
  language text not null,            -- 'fr', 'en', 'es', 'de', 'pt'
  level text not null default 'A1',  -- 'A1'-'C2'
  words_learned int default 0,
  grammar_topics_completed int default 0,
  conversations_count int default 0,
  exercises_solved int default 0,
  created_at timestamptz default now(),
  updated_at timestamptz default now(),

  unique(user_id, language)
);
```

### Миграция существующих данных

- Для каждого пользователя с `french_level` — создать запись `(user_id, 'fr', french_level)`
- Пересчитать счётчики из существующих таблиц (`vocabulary_progress`, `grammar_cards`, `conversations`)
- Поле `french_level` в `user_profiles` оставить для обратной совместимости, но перестать использовать в коде

### Локальный кеш (Dexie)

```typescript
interface UserLanguageSetting {
  id: string
  userId: string
  language: Language
  level: LanguageLevel
  wordsLearned: number
  grammarTopicsCompleted: number
  conversationsCount: number
  exercisesSolved: number
  updatedAt: Date
}
```

## Настройки — карточки языков

Раздел "Мои языки" в SettingsScreen. Каждый язык — карточка:

```
┌─────────────────────────────────┐
│  🇫🇷  Французский          A2  │
│  ───────────────────────────    │
│  Слов: 45  Грамматика: 3/7     │
│  [Изменить уровень]  [Удалить] │
└─────────────────────────────────┘
┌─────────────────────────────────┐
│  🇬🇧  Английский           B1  │
│  ───────────────────────────    │
│  Слов: 12  Грамматика: 1/6     │
│  [Изменить уровень]  [Удалить] │
└─────────────────────────────────┘

        [ + Добавить язык ]
```

### Изменить уровень

Раскрывает внутри карточки ряд кнопок A1-C2, выбранный подсвечен. Сохранение мгновенное.

### Удалить язык

Модальное подтверждение: "Удалить английский? Прогресс сохранится и будет доступен при повторном добавлении." Удаление не стирает данные из `vocabulary_progress`, `grammar_cards` и т.д. — только убирает запись из `user_language_settings`.

### Добавить язык

Модалка/боттомшит:

1. Список доступных языков (флаг + название), недоступные серые с лейблом "Скоро"
2. Нажал на язык — появляется выбор уровня A1-C2
3. Нажал уровень — язык добавлен, карточка появляется в списке

Реализованные языки: fr, en. Остальные (es, de, pt) — неактивны.

## Переключение языка и адаптация экранов

### Хедер — LanguageSwitcher

Дропдаун рядом с именем пользователя. Доработки:

- Показывать только языки из `user_language_settings` (не все доступные)
- Рядом с флагом показывать текущий уровень: `🇫🇷 A2` / `🇬🇧 B1`
- При переключении — загружать `level` из `user_language_settings` для выбранного языка

### Что происходит при переключении

```
Пользователь нажал 🇬🇧 B1 в хедере
        ↓
AuthContext.setCurrentLanguage('en')
        ↓
Загрузка настроек из user_language_settings: { language: 'en', level: 'B1', ... }
        ↓
Новый контекст: { currentLanguage: 'en', currentLevel: 'B1' }
        ↓
Все экраны перерисовываются
```

### Что меняется на экранах

| Экран | Что адаптируется |
|-------|-----------------|
| Главная | Статистика и прогресс для выбранного языка и уровня |
| Словарь | Слова из `vocabulary-en.json`, прогресс по английскому |
| Грамматика | Темы из `grammar-topics-en.json`, карточки по английскому |
| Упражнения | Задания из `exercises-en.json`, фильтр по уровню B1 |
| Разговор | Промпты и темы на английском |

### AuthContext — новые поля

```typescript
// Сейчас:
currentLanguage: Language

// Станет:
currentLanguage: Language
currentLevel: LanguageLevel       // из user_language_settings
currentLanguageStats: {           // из user_language_settings
  wordsLearned: number
  grammarTopicsCompleted: number
  conversationsCount: number
  exercisesSolved: number
}
```

Все компоненты, которые сейчас читают `profile.french_level`, переходят на `currentLevel` из контекста.

## Изменения в существующем коде

### Новые файлы

```
supabase/migrations/XXXX-user-language-settings.sql  — создание таблицы + миграция данных
src/types/supabase.ts                                — тип UserLanguageSetting (дополнение)
```

### Изменяемые файлы

```
src/contexts/AuthContext.tsx              — добавить currentLevel, currentLanguageStats,
                                           загрузку из user_language_settings при смене языка
src/services/userDataService.ts          — CRUD для user_language_settings:
                                           getLanguageSettings(), addLanguage(),
                                           removeLanguage(), updateLevel(),
                                           incrementStats()
src/types/index.ts                       — добавить UserLanguageSetting интерфейс,
                                           убрать зависимость от FrenchLevel где захардкожено
src/components/ui/LanguageSwitcher.tsx    — показывать только языки пользователя + уровень
src/components/Settings/SettingsScreen.tsx — заменить выбор уровня на карточки языков
src/components/Home/HomeScreen.tsx        — читать stats из currentLanguageStats
src/hooks/useHomeStats.ts                — брать level из currentLevel вместо french_level
src/components/Onboarding/OnboardingFlow.tsx — при онбординге создавать запись
                                               в user_language_settings вместо french_level
```

### Ключевой рефакторинг

Глобальный поиск и замена `profile.french_level` на `currentLevel` из AuthContext. Это ~10-15 мест в коде.

### Что НЕ меняется

- Модули (`VocabularyEngine`, `GrammarEngine`, `SRSEngine`) — уже принимают `language` параметром
- API-эндпоинты — уже поддерживают `language`
- Данные (`vocabulary-*.json`, `grammar-topics-*.json`) — без изменений
- Компоненты упражнений, словаря, грамматики — уже фильтруют по `currentLanguage`, нужно только заменить `french_level` на `currentLevel`
