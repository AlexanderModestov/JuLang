# JuLang - Дизайн приложения для изучения французского языка

**Дата:** 2026-01-11
**Версия:** 1.0
**Статус:** Утверждено

## Обзор проекта

JuLang - это десктопное приложение для изучения французского языка с фокусом на разговорную практику и грамматику. Приложение использует AI для персонализированных диалогов и систему интервального повторения (SRS) для закрепления материала.

### Ключевые возможности

- Разговорная практика с AI учителем (текстовый и голосовой режимы)
- Краткие объяснения грамматики
- Система интервального повторения (Spaced Repetition)
- Поддержка уровней A1-C2
- Выбор тем для диалогов (свободный выбор или случайная тема)
- Облачная синхронизация прогресса

### Целевая аудитория

Русскоязычные пользователи, изучающие французский язык, которым не хватает разговорной практики и структурированного изучения грамматики.

---

## 1. Архитектура и технологический стек

### Технологический стек

- **Frontend:** React + TypeScript + TailwindCSS
- **Backend:** Tauri (Rust)
- **База данных:** Supabase (PostgreSQL) + локальный кеш (IndexedDB)
- **AI провайдеры:** OpenAI GPT-4 и Anthropic Claude (REST API)
- **Распознавание речи:** Web Speech API
- **Синтез речи:** Web Speech API + опционально ElevenLabs

### Архитектурные слои

**1. Presentation Layer (React)**
- UI компоненты
- Управление состоянием (Zustand/Redux)
- Routing

**2. Application Layer (TypeScript)**
- Бизнес-логика
- Координация между модулями
- Управление состоянием приложения

**3. Domain Layer (Модули)**
- `AIService` - интеграция с OpenAI/Claude
- `GrammarEngine` - база грамматических правил
- `SRSEngine` - алгоритм spaced repetition (SM-2)
- `SpeechService` - распознавание и синтез речи
- `TopicManager` - управление темами диалогов

**4. Infrastructure Layer (Rust/Tauri)**
- Tauri команды (IPC)
- Файловая система
- Безопасное хранение API ключей (Keychain)
- Системные уведомления

**5. Data Layer**
- Supabase клиент
- Локальный кеш (IndexedDB)
- Синхронизация данных

### Коммуникация

Frontend → Tauri IPC → Rust Backend → Внешние API (OpenAI/Claude/Supabase)

---

## 2. Структура данных

### Supabase схема

#### Users (пользователи)
```sql
- id: uuid (PK)
- email: text
- name: text
- native_language: text (default: 'ru')
- french_level: text (A1-C2)
- preferred_ai_provider: text (openai/claude/random)
- created_at: timestamp
- updated_at: timestamp
```

#### Topics (темы для разговоров)
```sql
- id: uuid (PK)
- user_id: uuid (FK → users.id)
- title: text
- category: text (еда, путешествия, работа, хобби и т.д.)
- level: text (A1-C2)
- is_system: boolean (системная или пользовательская тема)
- created_at: timestamp
```

#### Conversations (диалоги)
```sql
- id: uuid (PK)
- user_id: uuid (FK → users.id)
- topic_id: uuid (FK → topics.id)
- ai_provider: text (openai/claude)
- mode: text (text/voice/mixed)
- started_at: timestamp
- ended_at: timestamp
- messages: jsonb (массив сообщений)
- quality_score: integer (0-100, оценка AI)
```

#### Grammar_Cards (грамматические карточки для SRS)
```sql
- id: uuid (PK)
- user_id: uuid (FK → users.id)
- topic: text (например: "Passé Composé")
- level: text (A1-C2)
- explanation: text
- examples: jsonb (массив примеров)
- next_review: timestamp
- ease_factor: float
- interval: integer (дни)
- repetitions: integer
- created_at: timestamp
- last_reviewed: timestamp
- practice_stats: jsonb (статистика по типам практики)
```

**Структура practice_stats:**
```json
{
  "written_translation": {
    "attempts": 15,
    "correct": 12,
    "last_attempt": "2026-01-10T14:30:00Z"
  },
  "repeat_aloud": {
    "attempts": 8,
    "avg_pronunciation_score": 78,
    "last_attempt": "2026-01-10T14:35:00Z"
  },
  "oral_translation": {
    "attempts": 5,
    "correct": 4,
    "avg_pronunciation_score": 82,
    "last_attempt": "2026-01-09T10:00:00Z"
  },
  "grammar_dialog": {
    "sessions": 3,
    "total_messages": 24,
    "grammar_usage_rate": 0.85,
    "last_session": "2026-01-08T18:00:00Z"
  }
}
```

#### Practice_Sessions (сессии практики)
```sql
- id: uuid (PK)
- user_id: uuid (FK → users.id)
- card_id: uuid (FK → grammar_cards.id)
- practice_type: text (written_translation/repeat_aloud/oral_translation/grammar_dialog)
- started_at: timestamp
- ended_at: timestamp
- exercises_completed: integer
- correct_answers: integer
- avg_pronunciation_score: float (nullable)
- final_quality: integer (0-5, для SRS)
- details: jsonb (детали каждого упражнения)
```

#### User_Progress (статистика)
```sql
- user_id: uuid (PK, FK → users.id)
- total_conversations: integer
- total_messages_sent: integer
- topics_covered: jsonb (массив topic_id)
- grammar_cards_mastered: integer
- current_streak: integer (дней подряд)
- last_activity_date: date
```

### Локальный кеш (IndexedDB)

- Последние 10 диалогов
- Карточки на повторение сегодня
- Настройки пользователя
- Офлайн очередь для синхронизации

---

## 3. Основные модули

### AIService

**Ответственность:** Единая точка взаимодействия с AI провайдерами.

**Функции:**
- `selectProvider(preference: string): Provider` - выбор OpenAI, Claude или случайный
- `createConversation(topic: string, level: string, context: object): Conversation` - создание системного промпта
- `sendMessage(message: string, history: Message[]): Promise<AIResponse>` - отправка сообщения
- `analyzeUserMessage(message: string): Promise<Analysis>` - анализ грамматических ошибок
- `generateGrammarExplanation(topic: string, level: string): Promise<Explanation>` - генерация объяснения
- `evaluateConversationQuality(conversation: Conversation): Promise<number>` - оценка качества (0-100)

**Промпт стратегия:**

AI выступает как терпеливый франкоговорящий учитель, который:
- Адаптируется к уровню пользователя (A1-C2)
- Мягко исправляет ошибки в следующем ответе
- Поддерживает естественный диалог на выбранную тему
- Периодически вводит новую лексику подходящего уровня
- Не использует родной язык пользователя (только французский)

### GrammarEngine

**Ответственность:** Управление грамматическими темами.

**Функции:**
- `getGrammarTopicsByLevel(level: string): Topic[]` - список грамматики для уровня
- `createGrammarCard(topic: string, level: string): Promise<Card>` - создание карточки (вызывает AI)
- `getCardsDueToday(userId: string): Promise<Card[]>` - карточки на повторение
- `explainGrammarInContext(rule: string, userSentence: string): Promise<string>` - объяснение ошибки

**База грамматики:**

Статический JSON файл с категориями по уровням:
- A1: артикли, être/avoir, простые времена
- A2: прошедшие времена, местоимения
- B1: субжонктив, условное наклонение
- B2: согласование времён, косвенная речь
- C1-C2: редкие конструкции, стилистика

### SRSEngine

**Ответственность:** Алгоритм интервального повторения.

**Алгоритм:** SM-2 (SuperMemo 2)

**Функции:**
- `scheduleCard(cardId: string, quality: number): Promise<void>` - планирование следующего повторения
  - quality: 0-5 (0 = не помню, 5 = легко)
  - Обновляет: ease_factor, interval, next_review
- `getReviewQueue(userId: string, date: Date): Promise<Card[]>` - карточки на сегодня
- `calculateNextInterval(card: Card, quality: number): number` - расчет следующего интервала
- `resetCard(cardId: string): Promise<void>` - сброс прогресса карточки

**Логика интервалов:**
- Качество 0-2: снова сегодня (интервал = 0)
- Качество 3: через 1 день
- Качество 4: через текущий интервал × ease_factor
- Качество 5: через текущий интервал × ease_factor × 1.3

**Уведомления:**
- Ежедневное напоминание о повторениях (через Tauri notifications)
- Настраиваемое время уведомления

### SpeechService

**Ответственность:** Работа с голосом.

**Функции:**
- `startListening(language: string): void` - начать распознавание (Web Speech API)
- `stopListening(): void` - остановить распознавание
- `synthesizeSpeech(text: string, voice: string, rate: number): void` - озвучить текст
- `getAvailableVoices(language: string): Voice[]` - список французских голосов
- `transcribeAudio(audioBlob: Blob): Promise<string>` - транскрипция (fallback через OpenAI Whisper)

**Режимы:**
- **Текстовый:** только печать
- **Голосовой ввод:** говорит → текст → отправка AI
- **Полный голосовой:** говорит → AI отвечает голосом
- **Комбинированный:** пользователь выбирает на лету

**Опции:**
- Оценка произношения (опционально, через сравнение с Whisper транскрипцией)

### TopicManager

**Ответственность:** Управление темами для диалогов.

**Функции:**
- `getTopicsByCategory(category: string, level: string): Topic[]` - темы по категории
- `searchTopics(query: string, level: string): Topic[]` - поиск тем
- `generateRandomTopic(level: string): Topic` - случайная тема для уровня
- `createCustomTopic(title: string, category: string): Promise<Topic>` - создание своей темы

**Категории:**
- Еда и рестораны
- Путешествия
- Работа и карьера
- Хобби и досуг
- Культура и искусство
- Повседневная жизнь
- Новости и события

### PracticeEngine

**Ответственность:** Генерация и управление различными типами практики грамматических правил.

**Типы практики:**

1. **Письменный перевод (written_translation)**
   - AI генерирует предложение на русском языке с использованием целевого грамматического правила
   - Пользователь вводит перевод на французский
   - AI проверяет и даёт обратную связь

2. **Повторение вслух (repeat_aloud)**
   - AI генерирует и озвучивает предложение на французском
   - Пользователь повторяет вслух
   - Система сравнивает транскрипцию и оценивает произношение

3. **Устный перевод (oral_translation)**
   - AI озвучивает предложение на русском
   - Пользователь переводит устно на французский
   - Система проверяет грамматическую правильность и произношение

4. **Мини-диалог (grammar_dialog)**
   - AI ведёт короткий диалог, провоцирующий использование целевого правила
   - Пользователь отвечает голосом или текстом
   - AI мягко корректирует ошибки

**Функции:**
```typescript
type PracticeType = 'written_translation' | 'repeat_aloud' | 'oral_translation' | 'grammar_dialog';

interface PracticeExercise {
  type: PracticeType;
  grammar_topic: string;
  source_text?: string;      // Исходный текст (для перевода)
  target_text?: string;      // Целевой текст (для повторения)
  audio_url?: string;        // URL аудио
  hints?: string[];          // Подсказки
}

interface PracticeResult {
  is_correct: boolean;
  user_answer: string;
  pronunciation_score?: number;  // 0-100
  feedback: string;
  grammar_notes?: string;
}
```

- `generateExercise(cardId: string, type: PracticeType): Promise<PracticeExercise>` - генерация упражнения
- `checkWrittenAnswer(exercise: PracticeExercise, userAnswer: string): Promise<PracticeResult>` - проверка письменного ответа
- `checkSpokenAnswer(exercise: PracticeExercise, audioBlob: Blob): Promise<PracticeResult>` - проверка устного ответа
- `startGrammarDialog(cardId: string): Promise<DialogSession>` - начать мини-диалог
- `getPracticeStats(userId: string, cardId: string): Promise<PracticeStats>` - статистика по типам практики

### PronunciationAnalyzer

**Ответственность:** Анализ произношения пользователя.

**Функции:**
- `analyzeAudio(audioBlob: Blob, expectedText: string): Promise<PronunciationAnalysis>` - анализ аудио
- `getDetailedFeedback(analysis: PronunciationAnalysis): string` - детальная обратная связь

**Структура анализа:**
```typescript
interface PronunciationAnalysis {
  transcription: string;        // Что распознано
  expected_text: string;        // Что ожидалось
  overall_score: number;        // 0-100
  word_scores: {                // Оценка по словам
    word: string;
    score: number;
    issues?: string[];          // Проблемы (ударение, звуки)
  }[];
  suggestions: string[];        // Рекомендации
}
```

**Технология:**
- Первичная транскрипция: Web Speech API / OpenAI Whisper
- Анализ произношения: сравнение с эталоном через AI
- Детекция типичных ошибок русскоговорящих (носовые, r grasseyé, liaison)

---

## 4. UI/UX и основные экраны

### Home Screen (Главный экран)

**Компоненты:**
- Приветствие с именем пользователя
- Текущий стрик (дни подряд) с иконкой огня
- Карточки на повторение сегодня (количество + кнопка "Начать повторение")
- Большая кнопка "Начать разговор"
- Последние диалоги (список с темами и датами)
- Статистика: всего диалогов, изученных тем, освоенных грамматических правил

### Topic Selection Screen (Выбор темы)

**Компоненты:**
- Поиск темы (свободный текст)
- Категории (карточки с иконками)
- Популярные темы для текущего уровня
- Кнопка "Случайная тема" (с анимацией рулетки)
- Возможность создать свою тему (модальное окно)

### Conversation Screen (Экран диалога)

**Компоненты:**
- История сообщений (scrollable chat)
  - Пользователь: справа, синий цвет
  - AI учитель: слева, серый цвет
- Поле ввода текста / кнопка микрофона
- Переключатель режима (текст/голос)
- Кнопка "Подсказка" - AI даёт hint
- Боковая панель (опционально):
  - Мини-словарь (новые слова из диалога)
  - Грамматические заметки
- Кнопка "Завершить диалог"

**Индикаторы:**
- AI печатает... (typing indicator)
- Распознавание речи активно (анимация микрофона)
- AI думает... (при обработке запроса)

### Grammar Review Screen (Повторение грамматики)

**Компоненты:**
- Карточка с правилом (фронт) - крупный текст
- Кнопка "Показать ответ"
- Примеры и объяснение (бэк) - с подсветкой синтаксиса
- Кнопки оценки:
  - Снова (красный)
  - Сложно (оранжевый)
  - Нормально (жёлтый)
  - Легко (зелёный)
- Прогресс бар: X из Y карточек
- Статистика сессии (в конце)

### Grammar Practice Screen (Практика грамматики)

**Описание:** Экран активной практики грамматического правила с выбором типа упражнения.

**Компоненты:**

**Шапка:**
- Название правила (например: "Passé Composé")
- Уровень (A2)
- Кнопка закрытия

**Переключатель типов практики (табы):**
```
┌──────────┬───────────┬────────────┬─────────────┐
│✏️ Перевод│🎤Повторить│🎙️ Устный   │💬 Диалог    │
│          │  вслух    │  перевод   │             │
└──────────┴───────────┴────────────┴─────────────┘
```

**Контент (зависит от выбранного типа):**

1. **Письменный перевод:**
   - Предложение на русском (сгенерировано AI)
   - Поле ввода для французского перевода
   - Кнопка "Проверить"
   - Результат: правильно/неправильно + объяснение AI
   - Кнопка "Следующее"

2. **Повторить вслух:**
   - Кнопка воспроизведения аудио (французское предложение)
   - Кнопка записи микрофона
   - После записи: транскрипция + оценка произношения (0-100%)
   - Детальный фидбэк: какие слова произнесены правильно/неправильно
   - Советы по улучшению произношения

3. **Устный перевод:**
   - Кнопка воспроизведения аудио (русское предложение)
   - Кнопка записи микрофона
   - После записи: транскрипция + проверка грамматики
   - Оценка произношения
   - Эталонный перевод для сравнения

4. **Мини-диалог:**
   - История сообщений (как в Conversation Screen)
   - AI начинает диалог, провоцируя использование правила
   - Переключатель: голос / текст
   - AI даёт обратную связь по грамматике после каждого ответа
   - Индикатор "Использовано правило: ✅ / ❌"

**Футер:**
- Прогресс: "5/10 упражнений"
- Статистика сессии (правильных ответов, средняя оценка произношения)
- Кнопка "Завершить практику"

**После завершения:**
- Экран статистики сессии
- Общая оценка (влияет на SRS)
- Кнопки: "Продолжить практику" / "На главную"

### Settings Screen

**Секции:**

**1. Профиль:**
- Уровень французского (A1-C2) - dropdown
- Имя пользователя
- Email

**2. AI настройки:**
- Предпочитаемый провайдер (OpenAI/Claude/Случайный)
- API ключи (безопасное поле ввода)
  - Ссылки на инструкции получения ключей
- Тестовая кнопка для проверки ключей

**3. Голосовые настройки:**
- Выбор французского голоса
- Скорость речи (slider)
- Тестовая фраза для прослушивания

**4. Уведомления:**
- Включить/выключить напоминания
- Время напоминания (time picker)
- Напоминать каждый день / только в будни

**5. Данные:**
- Экспорт данных (JSON)
- Импорт данных
- Удалить все мои данные (с подтверждением)

### Дизайн принципы

- Минималистичный, не перегружен элементами
- Акцент на читаемости текста (шрифт 16-18px)
- Поддержка тёмной и светлой темы
- Клавиатурные shortcuts:
  - `Ctrl+N` - новый диалог
  - `Ctrl+R` - начать повторение
  - `Space` - показать ответ карточки
  - `Esc` - закрыть модальное окно

---

## 5. Пользовательские сценарии

### Flow 1: Первый запуск приложения

1. Пользователь запускает приложение
2. **Онбординг экран 1:** Добро пожаловать в JuLang
3. **Онбординг экран 2:** Выбор уровня французского (A1-C2) с описанием каждого
4. **Онбординг экран 3:** Ввод API ключей (OpenAI и/или Claude)
   - Подсказки где получить
   - Ссылки на документацию
5. **Онбординг экран 4:** Выбор предпочитаемого AI провайдера
6. **Онбординг экран 5:** Создание аккаунта Supabase или вход
7. Генерация первых 5 грамматических карточек для выбранного уровня
8. Переход на Home Screen

### Flow 2: Начало разговора

1. Home Screen → клик "Начать разговор"
2. Topic Selection Screen
3. Пользователь выбирает тему (поиск, категория, случайная, или создаёт свою)
4. Выбор режима (текст/голос/комбо)
5. Переход на Conversation Screen
6. AI приветствует на французском и начинает диалог на тему
7. Пользователь отвечает (текстом или голосом)
8. AI поддерживает диалог:
   - Задаёт вопросы
   - Мягко исправляет ошибки в следующем ответе
   - Вводит новую лексику подходящего уровня
9. Пользователь кликает "Завершить диалог"
10. AI оценивает качество диалога (0-100)
11. Предложение сохранить новые слова/фразы в карточки
12. Возврат на Home Screen

### Flow 3: Повторение грамматики (карточки)

1. Home Screen → клик "Начать повторение" (показывает количество карточек)
2. Grammar Review Screen
3. Показывается карточка (правило на фронте)
4. Пользователь вспоминает, нажимает "Показать ответ"
5. Видит объяснение + примеры на бэке
6. Оценивает сложность: Снова/Сложно/Нормально/Легко (0-5)
7. SRSEngine планирует следующее повторение
8. Следующая карточка
9. После всех карточек: экран статистики сессии
   - Сколько карточек повторено
   - Средняя сложность
   - Следующая сессия повторения
10. Возврат на Home Screen

### Flow 3.1: Активная практика грамматики

1. Home Screen → клик на карточку грамматики → "Практиковать"
   ИЛИ Grammar Review Screen → кнопка "Практиковать это правило"
2. Grammar Practice Screen открывается
3. По умолчанию выбран "Письменный перевод"
4. AI генерирует упражнение на выбранное правило

**Письменный перевод:**
5a. Показывается предложение на русском
6a. Пользователь вводит перевод на французский
7a. Клик "Проверить"
8a. AI проверяет и показывает результат + объяснение
9a. Клик "Следующее" → новое упражнение

**Повторение вслух:**
5b. AI озвучивает французское предложение
6b. Пользователь нажимает "Записать" и повторяет вслух
7b. Система транскрибирует и анализирует произношение
8b. Показывается оценка (0-100%) и детальный фидбэк
9b. Советы по улучшению произношения

**Устный перевод:**
5c. AI озвучивает предложение на русском
6c. Пользователь переводит устно на французский
7c. Система проверяет грамматику + произношение
8c. Показывается эталонный перевод для сравнения

**Мини-диалог:**
5d. AI начинает диалог, провоцируя использование правила
6d. Пользователь отвечает (голосом или текстом)
7d. AI продолжает диалог, мягко корректируя ошибки
8d. Индикатор показывает, использовал ли пользователь правило

**Переключение между типами:**
- Пользователь может в любой момент переключиться на другой тип практики
- Прогресс сохраняется для всех типов

**Завершение:**
10. Клик "Завершить практику"
11. Экран статистики сессии:
    - Всего упражнений выполнено
    - Правильных ответов (%)
    - Среднее произношение (для голосовых)
    - Статистика по типам практики
12. Общая оценка влияет на SRS интервал карточки
13. Кнопки: "Продолжить практику" / "На главную"

### Flow 4: Создание новой грамматической карточки

1. **Во время диалога:** пользователь делает ошибку
2. AI замечает и в следующем ответе мягко исправляет:
   - "Ah, tu veux dire 'je suis allé' (passé composé avec être). Bonne tentative!"
3. Появляется всплывающее уведомление: "Добавить Passé Composé в карточки для повторения?"
4. Пользователь кликает "Да"
5. Карточка создаётся автоматически:
   - Тема: Passé Composé
   - Объяснение: сгенерировано AI на основе ошибки
   - Примеры: из контекста диалога
6. Планируется первое повторение через 1 день
7. Уведомление: "Карточка добавлена!"

### Flow 5: Офлайн режим

1. Пользователь теряет интернет соединение
2. Приложение переключается в "Offline Mode"
3. Показывает уведомление: "Работаем офлайн. Диалоги будут недоступны."
4. Доступные функции:
   - Повторение карточек (из локального кеша)
   - Просмотр старых диалогов
   - Изменение настроек
5. Недоступные функции (disabled):
   - Начать новый диалог
   - Создать новую карточку с AI
6. При восстановлении соединения:
   - Автоматическая синхронизация с Supabase
   - Уведомление: "Снова онлайн!"

---

## 6. Обработка ошибок и edge cases

### Ошибки API

**OpenAI/Claude API недоступен:**
- UI: Показать понятное сообщение: "Не удалось подключиться к [провайдер]. Проверьте интернет соединение."
- Действие: Предложить переключиться на другой провайдер
- Fallback: Сохранить сообщение локально для повторной отправки
- Если оба недоступны: режим "только повторение карточек"

**Неверный API ключ:**
- Проверка при вводе (тестовый запрос к API)
- UI: "API ключ невалиден. Проверьте правильность ключа."
- Действие: Ссылка на инструкцию получения ключа
- Блокировка функций, требующих API

**Превышен лимит API (rate limit):**
- UI: "Превышен лимит запросов к [провайдер]. Попробуйте через [время]."
- Действие: Показать оставшиеся запросы (если API поддерживает)
- Fallback: Предложить переключиться на другой провайдер
- Сохранить диалог и предложить продолжить позже

**Таймаут API:**
- Ожидание: 30 секунд
- UI: "Запрос выполняется дольше обычного..."
- После таймаута: "Не удалось получить ответ. Попробуйте ещё раз."
- Retry логика: автоматический повтор с exponential backoff

### Ошибки Supabase

**Supabase недоступен:**
- Все данные кешируются локально в IndexedDB
- UI: Индикатор "Офлайн режим"
- При восстановлении: автоматическая синхронизация
- Критичные операции (SRS) работают на локальном кеше

**Ошибка синхронизации:**
- Retry: 3 попытки с экспоненциальной задержкой
- Если не удалось: сохранить в очередь для синхронизации позже
- UI: "Не удалось синхронизировать данные. Повторим позже."

**Конфликт данных:**
- Стратегия: last-write-wins (последняя запись побеждает)
- Для критичных данных (SRS): использовать версионирование

### Ошибки распознавания речи

**Web Speech API не распознал речь:**
- UI: "Не расслышал, повторите пожалуйста"
- Действие: Микрофон остаётся активным
- Fallback: Кнопка переключения на текстовый ввод

**Распознан неправильный язык:**
- Проверка: детекция языка на стороне клиента
- Если не французский: "Пожалуйста, говорите на французском"

**Микрофон недоступен:**
- UI: "Микрофон недоступен. Проверьте разрешения."
- Действие: Ссылка на настройки системы
- Fallback: Переключение на текстовый режим

### Edge cases

**Пользователь не говорит ничего (таймаут):**
- Ожидание: 30 секунд тишины
- UI: "Вы ещё здесь?"
- Действие: Автоматическое сохранение диалога
- Микрофон деактивируется

**AI генерирует неподходящий контент:**
- Фильтры в системном промпте:
  - "You are a professional French language teacher. Keep all content appropriate for learners of all ages."
- UI: Кнопка "Пожаловаться на ответ"
- Логирование для улучшения промптов

**Нет карточек на повторение:**
- UI: "Нет карточек на сегодня! 🎉"
- Действие 1: Предложить создать новые из последних диалогов
- Действие 2: Предложить начать новый диалог

**Пользователь быстро меняет уровень:**
- Пересчитать подходящие грамматические топики
- UI: "Ваш уровень изменён. Адаптируем контент..."
- Действие: Предложить архивировать слишком лёгкие карточки

**Очень длинный диалог (>50 сообщений):**
- Ограничение контекста: последние 10-15 сообщений для AI
- UI: Индикатор "Длинный диалог может влиять на качество ответов"
- Действие: Предложить начать новый диалог на ту же тему

**Пользователь удаляет API ключи:**
- Проверка перед диалогом
- UI: "Для разговорной практики нужны API ключи"
- Блокировка функций диалога
- Повторение карточек остаётся доступным

---

## 7. Безопасность и конфиденциальность

### Хранение API ключей

**Tauri Keychain Integration:**

Используем системное хранилище учётных данных:
- **Windows:** Windows Credential Manager
- **macOS:** Keychain
- **Linux:** Secret Service API (libsecret)

**Rust команды:**
```rust
#[tauri::command]
async fn store_api_key(provider: String, key: String) -> Result<(), String>

#[tauri::command]
async fn get_api_key(provider: String) -> Result<String, String>

#[tauri::command]
async fn delete_api_key(provider: String) -> Result<(), String>
```

**Принципы:**
- API ключи НИКОГДА не хранятся в plain text
- Не хранятся в frontend коде или localStorage
- Доступ только через Tauri команды из Rust бэкенда
- Шифрование при передаче между процессами (IPC)

### Данные пользователя

**Supabase Row Level Security (RLS):**

Политики безопасности:
```sql
-- Пользователь видит только свои данные
CREATE POLICY "Users can view own data"
  ON conversations FOR SELECT
  USING (auth.uid() = user_id);

CREATE POLICY "Users can insert own data"
  ON conversations FOR INSERT
  WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can update own data"
  ON conversations FOR UPDATE
  USING (auth.uid() = user_id);
```

**Локальный кеш (IndexedDB):**
- Шифрование чувствительных данных (AES-256)
- Автоматическая очистка старых диалогов (>30 дней)
- Возможность полной очистки через Settings

### Конфиденциальность диалогов

**Что отправляется AI:**
- Только текущий диалог (последние 10 сообщений для контекста)
- Уровень пользователя (A1-C2)
- Выбранная тема
- Системный промпт с инструкциями

**НЕ отправляется:**
- Email пользователя
- Имя пользователя
- Другие диалоги
- Статистика и прогресс
- Любые персональные данные

**Логирование:**
- Диалоги хранятся в Supabase под полным контролем пользователя
- Кнопка "Удалить все мои данные" в Settings
- Экспорт данных в JSON (GDPR compliance)
- Нет аналитики третьих сторон

### Сетевая безопасность

**HTTPS:**
- Все API запросы только через HTTPS
- Проверка SSL сертификатов

**Таймауты:**
- API запросы: 30 секунд
- Предотвращение зависания приложения

**Rate limiting:**
- Макс. 60 запросов/минуту к AI API
- Защита от случайных spam-запросов

**Retry логика:**
- Exponential backoff (1s, 2s, 4s, 8s)
- Макс. 3 попытки
- Не retry для ошибок 4xx (только 5xx и сетевые)

### Обновления приложения

**Tauri Auto-Updater:**
- Проверка обновлений при запуске
- Проверка подписи обновлений (code signing)
- Пользователь контролирует установку
- Уведомление о новых версиях с changelog

**Безопасность обновлений:**
- Только с официального GitHub Releases
- Проверка хэш-сумм
- Rollback в случае ошибки

---

## 8. Тестирование

### Unit тесты

**Frontend (Jest/Vitest):**

**Модули для тестирования:**
- `AIService`: выбор провайдера, форматирование запросов
- `SRSEngine`: расчет интервалов, планирование повторений
- `GrammarEngine`: получение топиков по уровню
- `TopicManager`: поиск, фильтрация тем
- Утилиты: форматирование дат, валидация

**Примеры тестов:**
```typescript
describe('SRSEngine', () => {
  test('calculateNextInterval with quality 0 returns 0', () => {
    const card = { interval: 5, ease_factor: 2.5 };
    expect(calculateNextInterval(card, 0)).toBe(0);
  });

  test('calculateNextInterval with quality 5 increases interval', () => {
    const card = { interval: 5, ease_factor: 2.5 };
    expect(calculateNextInterval(card, 5)).toBeGreaterThan(5);
  });
});

describe('AIService', () => {
  test('selectProvider with "random" returns OpenAI or Claude', () => {
    const provider = selectProvider('random');
    expect(['openai', 'claude']).toContain(provider);
  });
});
```

**Покрытие:** >80% для бизнес-логики

**Backend (Rust - Cargo test):**

**Модули для тестирования:**
- Tauri команды (store_api_key, get_api_key)
- Работа с keychain
- Файловая система
- Валидация данных

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_store_and_get_api_key() {
        let provider = "openai".to_string();
        let key = "test-key-123".to_string();

        store_api_key(provider.clone(), key.clone()).unwrap();
        let retrieved = get_api_key(provider).unwrap();

        assert_eq!(retrieved, key);
    }
}
```

### Integration тесты

**E2E тесты (Playwright для Tauri):**

**Сценарии:**
1. Полный onboarding flow
2. Создание диалога с моком AI
3. Повторение карточек
4. Изменение настроек
5. Офлайн режим

**Пример:**
```typescript
test('user can complete onboarding', async ({ page }) => {
  await page.goto('/');
  await page.click('text=Начать');
  await page.selectOption('#level', 'B1');
  await page.fill('#openai-key', 'test-key');
  await page.click('text=Далее');
  // ...
  await expect(page).toHaveURL('/home');
});
```

**Моки:**
- OpenAI/Claude API моки для предсказуемых тестов
- Supabase локальный инстанс (Docker)
- Фиксированные даты для SRS тестов

**API интеграция:**

**Стратегия:**
- Development: моки для быстрой разработки
- CI/CD: опционально тесты с реальными API (с тестовыми ключами)
- Production: мониторинг реальных запросов

### Supabase интеграция

**Локальное тестирование:**
- Supabase CLI + Docker для локального инстанса
- Миграции схемы через SQL файлы
- Проверка RLS политик

```bash
# Запуск локального Supabase
supabase start

# Тестирование миграций
supabase db reset
```

### Тестирование Speech API

**Стратегия:**
- Моки для Web Speech API (не доступен в headless браузере)
- Тестовые аудио файлы для транскрипции
- Проверка fallback логики

### Manual тестирование

**Чек-лист перед релизом:**

**Функциональность:**
- [ ] Онбординг работает корректно
- [ ] Все flows (диалог, повторение, настройки) работают
- [ ] Оба AI провайдера (OpenAI, Claude) работают
- [ ] Голосовой ввод/вывод функционирует
- [ ] SRS правильно планирует повторения
- [ ] Офлайн mode сохраняет данные
- [ ] Синхронизация с Supabase работает

**UI/UX:**
- [ ] Тёмная/светлая тема корректно отображаются
- [ ] Клавиатурные shortcuts работают
- [ ] Адаптивность (разные размеры окна)
- [ ] Анимации плавные

**Безопасность:**
- [ ] API ключи хранятся в keychain
- [ ] RLS политики работают
- [ ] Нет утечек данных в логах

**Производительность:**
- [ ] Время загрузки приложения <3 секунд
- [ ] Отклик UI <100ms
- [ ] Размер установщика <30MB
- [ ] Использование памяти <200MB

**Кроссплатформенность:**
- [ ] Windows 10/11
- [ ] macOS (Intel + Apple Silicon)
- [ ] Linux (Ubuntu, Fedora)

### Performance тесты

**Метрики:**
- Время загрузки приложения: <3 секунд
- Время отклика UI на действия: <100ms
- Время отправки сообщения AI: <2 секунд (зависит от API)
- Размер установщика:
  - Windows: ~15-25 MB
  - macOS: ~10-20 MB
  - Linux: ~15-25 MB
- Использование памяти: <200MB в idle, <300MB во время диалога

**Инструменты:**
- Chrome DevTools для frontend
- Rust профилирование для backend
- Lighthouse для общей производительности

---

## 9. Развертывание

### Сборка приложения

**Команды:**
```bash
# Development build
npm run tauri dev

# Production build
npm run tauri build
```

**Генерация установщиков:**
- **Windows:** `.msi` (установщик) и `.exe` (portable)
- **macOS:** `.dmg` (образ диска) и `.app` (приложение)
- **Linux:** `.AppImage` (portable) и `.deb` (Ubuntu/Debian)

### CI/CD (GitHub Actions)

**Workflow файл:** `.github/workflows/release.yml`

**Этапы:**

**1. На каждый Pull Request:**
- Запуск линтеров (ESLint, Clippy)
- Запуск unit тестов (Jest, Cargo test)
- Проверка типов (TypeScript)
- Проверка форматирования (Prettier, rustfmt)

**2. На push в `main`:**
- Всё из PR
- E2E тесты (Playwright)
- Сборка для всех платформ (Windows, macOS, Linux)

**3. На создание тега (v1.0.0):**
- Всё из `main`
- Генерация changelog
- Создание GitHub Release
- Загрузка артефактов (установщики)
- Публикация обновления для auto-updater

**Матрица платформ:**
```yaml
strategy:
  matrix:
    platform: [ubuntu-latest, macos-latest, windows-latest]
```

### Подписание приложений

**Windows:**
- Code signing certificate (опционально для начала)
- Без подписи: SmartScreen предупреждение

**macOS:**
- Apple Developer certificate (обязательно для распространения)
- Notarization через `notarytool`
- Без подписи: Gatekeeper блокирует

**Linux:**
- Подписание не требуется

**Для MVP:** можно начать без подписи, добавить позже.

### Распространение

**Основной канал: GitHub Releases**
- Бесплатно
- Автоматические обновления через Tauri updater
- Changelog для каждой версии

**Дополнительные каналы (будущее):**
- Собственный сайт с landing page
- Microsoft Store (Windows)
- Mac App Store (macOS)
- Flathub (Linux)

### Auto-update

**Tauri Updater конфигурация:**

**tauri.conf.json:**
```json
{
  "tauri": {
    "updater": {
      "active": true,
      "endpoints": [
        "https://github.com/username/julang/releases/latest/download/latest.json"
      ],
      "dialog": true,
      "pubkey": "YOUR_PUBLIC_KEY"
    }
  }
}
```

**Процесс обновления:**
1. Приложение проверяет обновления при запуске
2. Если доступна новая версия:
   - Показывает диалог с changelog
   - Пользователь может обновить или отложить
3. Скачивание обновления в фоне
4. Проверка подписи
5. Установка и перезапуск

**Безопасность:**
- Обновления подписаны (публичный ключ в приложении)
- Только с официального GitHub
- Проверка хэш-сумм

---

## 10. Будущие улучшения (Roadmap)

### v1.0 - MVP (Initial Release)

**Дата:** Q2 2026

**Функции:**
- Разговорная практика с AI (текст + голос)
- Grамматические карточки + SRS
- Выбор тем для диалогов
- Поддержка уровней A1-C2
- Облачная синхронизация (Supabase)
- Оба AI провайдера (OpenAI, Claude)

### v1.1 - Расширенная аналитика

**Дата:** Q3 2026

**Функции:**
- Графики прогресса:
  - Количество диалогов по неделям
  - Слова в минуту (WPM)
  - Сложность диалогов
- Топ грамматических ошибок пользователя
- Рекомендации на основе ошибок:
  - "Вам стоит повторить Passé Composé"
- Экспорт статистики (PDF, CSV)

### v1.2 - Социальные фичи

**Дата:** Q4 2026

**Функции:**
- Система достижений (badges):
  - "Первый диалог"
  - "7 дней подряд"
  - "100 карточек освоено"
- Таблица лидеров (опционально, с приватностью)
- Поделиться прогрессом (скриншот статистики)
- Друзья и челленджи

### v1.3 - Расширение контента

**Дата:** Q1 2027

**Функции:**
- Поддержка других языков:
  - Испанский
  - Немецкий
  - Итальянский
- Видео/аудио материалы:
  - Интеграция с YouTube (французские каналы)
  - Подкасты с транскрипцией
- Новости на французском:
  - Ежедневные статьи адаптированные по уровню
  - Обсуждение новостей с AI

### v1.4 - Продвинутые фичи AI

**Дата:** Q2 2027

**Функции:**
- Оценка произношения с детальным фидбеком:
  - Интонация
  - Акцент
  - Скорость
- Персонализированный учебный план:
  - AI анализирует слабые места
  - Генерирует индивидуальный план
- Ролевые игры:
  - Собеседование на работу
  - В ресторане
  - У врача

### v1.5 - Интеграции

**Дата:** Q3 2027

**Функции:**
- Интеграция с внешними словарями (Reverso, WordReference)
- Импорт карточек из Anki
- Экспорт в популярные форматы (Quizlet, Memrise)
- API для расширений

### v2.0 - Мобильная версия

**Дата:** Q4 2027

**Платформы:**
- iOS
- Android

**Технологии:**
- React Native или Flutter
- Синхронизация с desktop версией через Supabase
- Push уведомления
- Офлайн режим

**Дополнительные фичи:**
- Виджет на главный экран (статистика)
- Быстрый доступ к повторению
- Голосовые сообщения в диалогах

### v2.1 - Платные функции (Monetization)

**Дата:** Q1 2028

**Модель:**
- Freemium: базовые функции бесплатно
- Premium подписка ($9.99/месяц):
  - Безлимитные диалоги
  - Приоритетная поддержка
  - Продвинутая аналитика
  - Оценка произношения
  - Ранний доступ к новым фичам
- Lifetime лицензия ($99.99)

**Дополнительно:**
- Корпоративные лицензии для школ
- B2B API для интеграции

---

## 11. Метрики успеха (KPIs)

### Пользовательские метрики

**Retention (удержание):**
- Day 1: 60%+ пользователей возвращаются на следующий день
- Day 7: 40%+ пользователей возвращаются через неделю
- Day 30: 25%+ пользователей возвращаются через месяц

**Engagement (вовлечённость):**
- Среднее 3+ диалога в неделю на активного пользователя
- Среднее 20+ карточек повторено в неделю
- Средняя длина диалога: 10+ сообщений

**Learning outcomes (результаты обучения):**
- 60%+ пользователей повышают уровень через 3 месяца (A1→A2, B1→B2 и т.д.)
- Средняя оценка качества диалога AI: 70+ (из 100)
- 80%+ пользователей находят объяснения грамматики полезными (опрос)

### Технические метрики

**Производительность:**
- Время загрузки: <3 секунд (p95)
- Время отклика UI: <100ms (p95)
- API latency: <2 секунды (p95)
- Crash rate: <0.1%

**Качество:**
- Test coverage: >80%
- Bug rate: <5 критичных багов на релиз
- Uptime Supabase: >99.9%

### Бизнес метрики (для будущего)

**Рост:**
- MAU (Monthly Active Users): +20% рост каждый квартал
- Новые регистрации: 1000+ в первый месяц после запуска

**Конверсия (для v2.1):**
- Free → Premium: 5%+
- Чurn rate: <5% в месяц

---

## 12. Риски и митигации

### Технические риски

**Риск 1: API провайдеры меняют цены/условия**
- Вероятность: Средняя
- Влияние: Высокое
- Митигация:
  - Поддержка нескольких провайдеров
  - Возможность добавить локальные модели (Ollama)
  - Пользователи сами платят за API

**Риск 2: Web Speech API не работает на всех платформах**
- Вероятность: Низкая
- Влияние: Среднее
- Митигация:
  - Fallback на OpenAI Whisper API
  - Текстовый режим всегда доступен

**Риск 3: Supabase outage**
- Вероятность: Низкая
- Влияние: Среднее
- Митигация:
  - Локальный кеш для критичных данных
  - Офлайн режим
  - Автоматическая синхронизация при восстановлении

### Продуктовые риски

**Риск 1: Пользователям не нравится AI диалоги**
- Вероятность: Средняя
- Влияние: Высокое
- Митигация:
  - Тестирование с фокус-группой
  - Итерация промптов на основе фидбека
  - Добавить заранее подготовленные диалоги

**Риск 2: Низкая retention из-за сложности**
- Вероятность: Средняя
- Влияние: Высокое
- Митигация:
  - Простой onboarding
  - Gamification (достижения, streak)
  - Напоминания

**Риск 3: Конкуренты (Duolingo, Babbel)**
- Вероятность: Высокая
- Влияние: Среднее
- Митигация:
  - Фокус на разговорную практику (USP)
  - Персонализация через AI
  - Open source / бесплатная модель

---

## 13. Структура проекта

```
JuLang/
├── src/                          # Frontend код
│   ├── components/               # React компоненты
│   │   ├── Home/
│   │   ├── Conversation/
│   │   ├── GrammarReview/
│   │   ├── GrammarPractice/      # Новый: практика грамматики
│   │   │   ├── PracticeScreen.tsx           # Основной экран практики
│   │   │   ├── PracticeTypeTabs.tsx         # Переключатель типов практики
│   │   │   ├── WrittenTranslation.tsx       # Письменный перевод
│   │   │   ├── RepeatAloud.tsx              # Повторение вслух
│   │   │   ├── OralTranslation.tsx          # Устный перевод
│   │   │   ├── GrammarDialog.tsx            # Мини-диалог на правило
│   │   │   ├── PronunciationFeedback.tsx    # Компонент фидбэка по произношению
│   │   │   ├── ExerciseResult.tsx           # Результат упражнения
│   │   │   └── PracticeStats.tsx            # Статистика сессии
│   │   ├── TopicSelection/
│   │   └── Settings/
│   ├── modules/                  # Бизнес-логика модули
│   │   ├── AIService.ts
│   │   ├── GrammarEngine.ts
│   │   ├── SRSEngine.ts
│   │   ├── SpeechService.ts
│   │   ├── TopicManager.ts
│   │   ├── PracticeEngine.ts     # Новый: генерация упражнений
│   │   └── PronunciationAnalyzer.ts  # Новый: анализ произношения
│   ├── store/                    # State management (Zustand)
│   ├── utils/                    # Утилиты
│   ├── types/                    # TypeScript типы
│   ├── hooks/                    # React hooks
│   ├── assets/                   # Статичные файлы
│   ├── styles/                   # Global CSS
│   └── main.tsx                  # Entry point
├── src-tauri/                    # Tauri/Rust код
│   ├── src/
│   │   ├── main.rs               # Главный файл
│   │   ├── commands/             # Tauri команды
│   │   │   ├── keychain.rs
│   │   │   ├── notifications.rs
│   │   │   └── fs.rs
│   │   └── lib.rs
│   ├── Cargo.toml
│   └── tauri.conf.json           # Конфигурация Tauri
├── docs/                         # Документация
│   ├── plans/                    # Дизайн документы
│   ├── api/                      # API документация
│   └── guides/                   # User guides
├── tests/                        # Тесты
│   ├── unit/                     # Unit тесты
│   ├── integration/              # Integration тесты
│   └── e2e/                      # E2E тесты (Playwright)
├── supabase/                     # Supabase конфигурация
│   ├── migrations/               # SQL миграции
│   └── seed.sql                  # Начальные данные
├── .github/
│   └── workflows/
│       └── release.yml           # CI/CD
├── public/                       # Публичные файлы
├── package.json
├── tsconfig.json
├── vite.config.ts
└── README.md
```

---

## Заключение

Этот дизайн документ описывает полную архитектуру JuLang - приложения для изучения французского языка с использованием AI и spaced repetition.

**Следующие шаги:**

1. Настроить проектную структуру (Tauri + React + TypeScript)
2. Настроить Supabase и создать схему БД
3. Создать основные модули (AIService, SRSEngine)
4. Реализовать MVP функциональность
5. Провести тестирование с фокус-группой
6. Итерировать на основе фидбека
7. Подготовить к релизу (CI/CD, подписание)

**Контакт для вопросов:** [добавить контакт]

---

**Утверждено:** 2026-01-11
**Версия:** 1.0
